# this a is simple log in test
"""for user name please type "morgan"
for password type "admin"
"""

import time


print ("hello, please enter your first name")

name=raw_input()

username= "morgan" or "Morgan" 

if name == "username"
	print "Hello Morgan, Please wait"
	time.sleep(2)
	print("logging in")
if name != "username"
	print ("incorrect password, closing application")
	time.sleep(2)
	exit